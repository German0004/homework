# Створіть модуль для отримання простих чисел. Імпортуйте його з іншого модуля. Імпортуйте його окремі імена.

# Пояснення:
# Створення модуля primes.py:

# is_prime(n): Функція перевіряє, чи є число простим.
# get_primes(limit): Функція повертає список простих чисел до певного значення.
# Імпортування модуля в main.py:

# Імпортується весь модуль за допомогою import primes.
# Функції з модуля використовуються через primes.get_primes() та primes.is_prime().
# Імпортуються окремі імена за допомогою from primes import is_prime, get_primes.
# Імпортовані функції використовуються напряму через get_primes() та is_prime().
# Таким чином, ви можете організувати свій код у модулі для отримання простих чисел і використовувати його в інших модулях,
# імпортуючи весь модуль або окремі імена.